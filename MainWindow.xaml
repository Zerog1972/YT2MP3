<Window x:Class="YT2MP3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Convertisseur YouTube vers MP3"
        Height="570"
        Width="1067"
        MinHeight="420"
        MinWidth="640"
        WindowStartupLocation="CenterScreen"
        Background="#F3F6FC"
        FontFamily="Segoe UI"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#4F46E5" />
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#4338CA" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB" />
        <SolidColorBrush x:Key="CardBrush" Color="#FCFFFFFF" />
        <SolidColorBrush x:Key="CardBorderBrush" Color="#DCE4F2" />
        <SolidColorBrush x:Key="TextMainBrush" Color="#111827" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#6B7280" />

        <DropShadowEffect x:Key="CardShadow"
                          Color="#260F172A"
                          BlurRadius="18"
                          ShadowDepth="4"
                          Opacity="0.45" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect" Value="{StaticResource CardShadow}" />
            <Setter Property="Padding" Value="16,14" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#F8FAFF" />
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}" />
            <Setter Property="BorderBrush" Value="#D5DEED" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="11,9" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                <Setter TargetName="Root" Property="Background" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.65" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                CornerRadius="9"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.985" ScaleY="0.985" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.58" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Foreground" Value="#334155" />
            <Setter Property="Background" Value="#EEF2FF" />
            <Setter Property="BorderBrush" Value="#D5DEED" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8FF" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Ellipse Width="260" Height="260"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Top"
                 Margin="-95,-120,0,0"
                 Fill="#3B6366F1"
                 IsHitTestVisible="False" />
        <Ellipse Width="220" Height="220"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Bottom"
                 Margin="0,0,-90,-100"
                 Fill="#2A60A5FA"
                 IsHitTestVisible="False" />

        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="4,0,4,12"/>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="URLs YouTube (une par ligne)"
                               Margin="2,0,0,9"
                               Foreground="{StaticResource TextMainBrush}"
                               FontWeight="SemiBold"
                               FontSize="13" />
                    <TextBox x:Name="TxtUrls"
                             Grid.Row="1"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             TextWrapping="NoWrap" />
                </Grid>
            </Border>

            <Border Grid.Row="2" Margin="0,11,0,0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Dossier de sortie"
                               Margin="2,0,0,9"
                               Foreground="{StaticResource TextMainBrush}"
                               FontWeight="SemiBold"
                               FontSize="13" />

                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="150" />
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="TxtOutputFolder" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />

                        <Button x:Name="BtnBrowse"
                                Grid.Row="0"
                                Grid.Column="2"
                                Height="38"
                                Content="Parcourir"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="BtnBrowse_Click" />

                        <ProgressBar x:Name="ConversionProgressBar"
                                     Grid.Row="1"
                                     Grid.Column="0"
                                     Margin="0,10,0,0"
                                     Height="38"
                                     Minimum="0"
                                     Maximum="100"
                                     Value="0"
                                     Foreground="{StaticResource AccentBrush}"
                                     Background="#EAF0FF"
                                     BorderThickness="0" />

                        <Button x:Name="BtnConvert"
                                Grid.Row="1"
                                Grid.Column="2"
                                Margin="0,10,0,0"
                                Height="38"
                                Content="Convertir"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Click="BtnConvert_Click" />
                    </Grid>
                </Grid>
            </Border>

            <StatusBar Grid.Row="3"
                       Margin="0,13,0,0"
                       Background="#EEF2FF"
                       BorderBrush="#D5DEED"
                       BorderThickness="1"
                       Padding="7,1">
                <StatusBarItem HorizontalAlignment="Stretch">
                    <DockPanel LastChildFill="True">
                        <TextBlock x:Name="TxtToolsVersion"
                                   DockPanel.Dock="Right"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   Margin="10,0,0,0" />
                        <Separator DockPanel.Dock="Right" Margin="10,0" />
                        <TextBlock x:Name="TxtStatus"
                                   Text="Initialisation..."
                                   Foreground="{StaticResource TextMainBrush}" />
                    </DockPanel>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</Window>